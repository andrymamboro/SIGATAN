import React from 'react';
import { View, Text, StyleSheet, ScrollView } from 'react-native';
import MainLayout from '../components/MainLayout';

export default function SuratPermohonanScreen({ route, navigation }) {
export default function SuratPermohonanScreen() { return null; }
}

const styles = StyleSheet.create({
// Styles removed as the component is now empty
});
